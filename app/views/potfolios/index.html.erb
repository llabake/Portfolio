<div class="album text-muted">
  <div class="container">

    <div class="row">
      <%=link_to "Create New Item", new_potfolio_url if logged_in?(:site_admin ) %>
      <%= render partial: "potfolio_item", collection: @potfolio_items %>
    </div>
  </div>
</div>
